type Wastebin @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  capacity: Int
  fillPercentage: Float
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
}
  