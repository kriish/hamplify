import React from "react";

const MyBins = ({ styles }) => {
  const viewStyle = {
    backgroundColor: "#c8e6c9",
  };

  return (
    <div style={viewStyle}>
      <h1>My Bins</h1>
    </div>
  );
};

export default MyBins;